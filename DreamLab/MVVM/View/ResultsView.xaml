<UserControl x:Class="DreamLab.MVVM.View.ResultsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             Background="#0F141A">

    <UserControl.Resources>
        <!-- Styl dla przycisków -->
        <Style x:Key="RoundedButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#1E88E5"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Padding" Value="10,5"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="8"
                                SnapsToDevicePixels="True">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#42A5F5"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1565C0"/>
                </Trigger>
            </Style.Triggers>
        </Style>
    </UserControl.Resources>

    <Grid Margin="30">
        <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top" Margin="0,10,0,0">

            <TextBlock Text="Ostatnie wyniki"
                       FontSize="30"
                       FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       Margin="0,0,0,20"/>

            <ListView ItemsSource="{Binding Results}"
                      Width="420"
                      Height="250"
                      Background="#1A1D23"
                      BorderThickness="0"
                      Foreground="White"
                      ScrollViewer.VerticalScrollBarVisibility="Auto">
                <ListView.View>
                    <GridView>
                        <GridViewColumn Header="Data" DisplayMemberBinding="{Binding Date}" Width="140"/>
                        <GridViewColumn Header="Czas" DisplayMemberBinding="{Binding Duration}" Width="100"/>
                        <GridViewColumn Header="Trafność (%)" DisplayMemberBinding="{Binding Accuracy}" Width="120"/>
                    </GridView>
                </ListView.View>
            </ListView>

            <!-- Tekst z podsumowaniem trafności -->
            <TextBlock Text="{Binding AverageAccuracyText}"
                       FontSize="18"
                       Foreground="#B0BEC5"
                       HorizontalAlignment="Center"
                       Margin="0,15,0,10"/>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,10,0,0">
                <Button Content="Wyczyść wyniki"
                        Command="{Binding ClearResultsCommand}"
                        Width="180"
                        Height="45"
                        Style="{StaticResource RoundedButtonStyle}"
                        Background="#E53935"/>
            </StackPanel>

        </StackPanel>
    </Grid>
</UserControl>
